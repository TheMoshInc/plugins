---
name: product-builder
description: MOSH の商品を MCP 経由で作成・更新・公開・削除するスキル。要件ヒアリング → 商品の作成または更新 → 読み戻してレビュー → ユーザー承認 → 公開状態の変更、という流れで対話的に進める。「商品を作って」「商品名を変えたい」「この商品を公開して」「非公開に戻して」「申し込み後の流れを設定して」「商品を削除して」「product-builder」など、MOSH の商品を変更するリクエストで使用する。「ちょっと説明文を直すだけ」のような小さな変更でも、書き込み系ツールを使う場合は本スキルを起動し、承認ルールを必ず適用する。
---

# MOSH 商品ビルダー

## Overview

MCP ツール（以下ツール名は OpenAPI の operationId で記す。実際のツール名にはサーバー識別子の接頭辞（例: `mcp__<server>__postCreatorProduct`）が付くが、その形は環境により異なるため本書では付けない）を使って、MOSH クリエイターの商品を作成・更新・公開・削除する。

**扱えるのは商品そのもの**（商品名・説明・画像・注意事項・キャンセルポリシー・申し込み後の流れ・公開状態）だけである。**プラン（価格・課金サイクル・定員等）の作成・変更・削除はこのスキルでは行えない**。プランは管理画面で操作する。

商品を公開するには**公開中のプランが 1 つ以上必要**なので、プランが 1 つも無い新規商品はこのスキルだけでは公開まで到達できない。この前提を最初にユーザーへ伝える。

リクエストの構造・制約は [references/content-schema.md](references/content-schema.md) を、ツールのパラメータは [references/mcp-tools.md](references/mcp-tools.md) を参照してから送信を組み立てる。

## 前提条件

| 必要なもの | MCP で実在確認できるか | できない場合の扱い |
|---|---|---|
| 商品 ID | できる（`getCreatorProducts` の一覧から名前で特定） | — |
| 商品画像の ID（`imageIds` / `steps[].imageId`） | **できない**（画像を一覧・検索するツールが無い） | ユーザーに直接確認する。既存商品の画像を残す場合のみ `getCreatorProduct` の値をそのまま使える |
| 公開中のプランの有無 | できる（`getCreatorProductPlans` で公開状態を確認） | — |
| 事業者情報の登録 | **できない**（確認するツールが無い） | 公開時に拒否されて初めて分かる。事前に断定しない |

## When to use

- 商品を新しく作りたいとき
- 既存商品の名前・説明・画像・注意事項・キャンセルポリシー・申し込み後の流れを変えたいとき
- 商品を公開・限定公開・非公開に切り替えたいとき
- 商品を削除したいとき

## When NOT to use

- 商品・プランの内容を**見るだけ** → `product-navigator` スキル
- プラン（価格・課金サイクル・定員・販売期間等）の作成・変更・削除 → 管理画面で行う
- 売上・購入者の確認 → `sales-reporter` スキル
- LP（ランディングページ）の作成・編集 → `lp-builder` スキル
- ワークフロー（ステップ配信） → `workflow-builder` スキル

## Workflow

### 1. 要件ヒアリング

ユーザーに以下を確認する（自明なら聞き直さない）:

- 新規作成か、既存商品の変更か、削除か
- 既存商品が対象なら、どの商品か（商品名・キーワードで良い。ID をユーザーに求めない）
- 変えたい項目と、その内容
- 公開状態を変えるか

新規作成なら、必須 4 項目（商品名・商品説明・注意事項・キャンセルポリシー）をこの段で揃える。**空欄を仮の文言で埋めない**。決まっていない項目があれば、そのまま聞く。

### 2. 対象商品の特定（更新・削除のとき）

1. `getCreatorProducts` で一覧を取得し、ユーザーが言った名前から対象を特定する
2. 完全一致しない場合は候補を提示して選んでもらう（**勝手に決めない**）
3. `getCreatorProduct` で現在の内容を取得し、**変更前の値を控える**。差分をユーザーに示すために使う

### 3. 作成または更新

**新規作成**は `postCreatorProduct` を呼ぶ。作成された商品は**必ず非公開**で始まる。作成時に公開状態は指定できない。

**更新**は `patchCreatorProduct` を呼ぶ。送ったフィールドだけが変わる。ただし次の 2 つは**配列まるごとの置き換え**になる:

- `imageIds` … 送った配列がそのまま新しい画像一覧になる。**1 枚追加したいときも、既存の ID を含めた全件を送る**
- `steps` … 同上。1 ステップだけ直したいときも全ステップを送る

既存の値を保つには `getCreatorProduct` で取得した値をそのまま含めて送る。ここを省くと**黙って消える**。

送信前に [references/best-practices.md](references/best-practices.md) 末尾のセルフレビューチェックリストを確認する。`steps` を送る場合は [examples/update-product-steps.json](examples/update-product-steps.json)、新規作成は [examples/create-product.json](examples/create-product.json) の形を参照する。

### 4. 読み戻してレビュー

保存に成功したら `getCreatorProduct` で**読み戻し**、意図どおり保存されたかを確認してからユーザーに報告する。とくに `imageIds` と `steps` は置き換えなので、**消えていないこと**をここで確かめる。

変更点を「変更前 → 変更後」の形で提示する。

### 5. 公開状態の変更（ユーザー承認が必須）

公開状態の変更は `patchCreatorProduct` に `publishingStatus` を渡して行う。**この操作の直前に必ずユーザーの承認を取る**。

公開（`PUBLIC` / `LIMITED`）にする前に確認すること:

1. `getCreatorProductPlans` で**公開中のプランが 1 つ以上あるか**を確認する。無ければ公開できないので、先に管理画面でプランを公開してもらう
2. プランの公開状態は**販売期間を反映した実効値**で判定される。販売期間が終了したプランは「公開中」に数えられない
3. 事業者情報が未登録だと拒否される。事前に確認する手段が無いため、拒否されたらその旨を伝える

非公開（`PRIVATE`）にするときは、次をユーザーに伝えてから承認を取る:

- **紐づくプランもすべて非公開になる**（公開中の別商品も同じ内容を参照しているプランは非公開にしない）
- 公開に戻しても、プランの公開設定は自動では戻らない

`PUBLIC` / `LIMITED` への変更ではプランの公開設定は変わらない。

変更後は `getCreatorProduct` で読み戻し、`publishingStatus` と `publicUrl` を確認して報告する。

### 6. 削除（ユーザー承認が必須）

削除は取り消せない。次の順で進める。

1. `getCreatorProduct` で対象を読み戻し、**商品名でユーザーに提示**して本当にこれで良いか確かめる
2. `isDeletable` を確認する。`false` なら購入者がいるため削除できない。**ツールを呼ばずに**その旨を伝える
3. 「この操作は取り消せないこと」「紐づくプランも同時に削除されること」を伝え、**明示的な承認を取る**
4. 承認を得てから `deleteCreatorProduct` を呼ぶ
5. `getCreatorProducts` で読み戻し、一覧から消えたことを確認して報告する

`isDeletable` が `true` でも、削除の直前に購入が入っていれば拒否される。拒否理由はそのまま伝える。

## ユーザーへの提示・コミュニケーション規約

1. JSON をそのまま見せない。表または箇条書きに整形して提示する（ユーザーが明示的に求めた時だけ JSON を出す。ただし生の内部 ID・リッチテキストの内部形式・enum の英字値は、明示的に求められても出さない）
2. HTTP 用語・ステータスコードを文面に出さない。「保存できました」「◯◯のため公開できませんでした」のように平易な日本語で伝える
3. 生の内部 ID（`id` / `productId` / `imageIds` の値 / `slug`）を提示に出さない。商品は商品名で示す。公開ページの案内には `publicUrl` を使う
4. enum 値は日本語に変換して出す（変換表は [references/content-schema.md](references/content-schema.md)）。`PUBLIC` 等の英字をそのまま見せない
5. `description` / `precautions` / `cancellationPolicy` はリッチテキストの内部形式で返ることがある。内部形式のまま見せず、本文テキストを取り出して提示する
6. 書き込みの前後で「何をこれから変えるか」「何が実際に変わったか」を必ず言葉にする。黙って保存しない

## 必須ルール

- **公開・非公開の切り替えと削除は、直前にユーザーの承認を取ってから実行する。** 依頼文に含まれていても、実行前にもう一度確かめる
- **保存したら必ず読み戻して確認する。** 保存が成功したことと、意図どおりの内容になっていることは別
- **`imageIds` と `steps` は全件送る。** 部分的に送ると送らなかったものが消える
- **画像 ID を捏造しない。** ユーザーから示された ID か、`getCreatorProduct` で取得した既存の値だけを使う。それらしい文字列を作らない
- **必須項目を仮の文言で埋めない。** 決まっていなければユーザーに聞く
- プランの作成・変更・削除を求められたら、このスキルではできない旨と管理画面での操作を案内する
- 拒否されたら再試行しない。返ってきた理由をそのまま平易に伝える

## よくあるミス

| NG | OK |
|---|---|
| 画像を 1 枚足すのに新しい ID だけを `imageIds` に送る | 既存の ID を含めた全件を送る。送らなかった画像は消える |
| ステップを 1 つ直すのに、そのステップだけ送る | 全ステップを送る。`steps` は置き換え |
| 作成直後に「公開しました」と報告する | 作成された商品は非公開。公開は別途 `publishingStatus` の更新が必要 |
| 公開できないので `PUBLIC` を何度も送り直す | 公開中のプランが無いか事業者情報が未登録。原因を伝えて管理画面へ案内する |
| `isDeletable: false` の商品に削除を実行して確かめる | 実行せずに「購入者がいるため削除できない」と伝える |
| 承認を取らずに非公開へ切り替える | 非公開にするとプランも非公開になることを伝えて承認を取る |
| `publicUrl` が `null` → 「URL の取得に失敗」 | 非公開の商品では公開ページが存在しないため `null` が正常 |
| `steps` の `imageId` が無いので項目ごと省く | `imageId` は省略できない。画像が無いなら `null` を入れる |
| 説明文が長いので適当に切り詰める | 表示テキストは 10,000 文字まで。超えるならユーザーに相談して削る |

## エラーが返ったとき

拒否された理由は返ってきたメッセージに入っている。**再試行せず**、理由をそのまま平易な日本語で伝える。主なものは [references/content-schema.md](references/content-schema.md) の「拒否されたときのメッセージ」にまとめてある。

存在しない商品や他のユーザーの商品を指定すると「見つからない」旨のエラーが返る。対象の特定からやり直すか、ユーザーに商品名を確認する。認証エラーの場合は、MOSH との接続（API トークンの設定）を見直すよう案内する。

## References

- [references/mcp-tools.md](references/mcp-tools.md) — 使用する MCP ツールのパラメータ詳細
- [references/content-schema.md](references/content-schema.md) — リクエスト・レスポンス構造・enum の日本語変換表・拒否メッセージ・絶対に避けること
- [references/best-practices.md](references/best-practices.md) — 組み立て方とセルフレビューチェックリスト
- [examples/create-product.json](examples/create-product.json) — 商品作成のリクエスト例
- [examples/update-product-steps.json](examples/update-product-steps.json) — 申し込み後の流れを含む更新のリクエスト例
